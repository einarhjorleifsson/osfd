% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_checks.R
\name{fd_check_tacsat}
\alias{fd_check_tacsat}
\title{Single-pass TACSAT quality checks ("myway")}
\usage{
fd_check_tacsat(tacsat, it_min = 5 * 60, area = ices_areas, harbour = harbours)
}
\arguments{
\item{tacsat}{A TACSAT data frame as returned by \code{fd_tacsat_clean()}. Must
contain \code{VE_COU}, \code{VE_REF}, \code{SI_DATIM} (POSIXct), \code{SI_LONG}, \code{SI_LATI},
and \code{SI_SP}.}

\item{it_min}{Numeric. Minimum permitted time interval between consecutive
pings for the same vessel, in \strong{seconds}. Default: \code{300} (5 minutes).}

\item{area}{A spatial containing area of interest. If NULL (default) no checking done.}

\item{harbour}{A spatial table containing list of harbours}
}
\value{
The input data frame with two new columns appended:
\describe{
\item{\code{INTV}}{Time since previous ping per vessel, in seconds. \code{NA} for
the first ping of each vessel (or where \code{SI_DATIM} is \code{NA}).}
\item{\code{checks}}{Character. First failing check label, or \code{"ok"}.}
}
}
\description{
Adds a \code{checks} column to a TACSAT data frame in a single \code{case_when()} pass,
labelling each record with the first failing check or \code{"ok"} if it passes all
of them. Also computes an \code{INTV} column (time in seconds since the previous
ping for the same vessel) which is used by the interval threshold check and is
useful downstream.

Expects a data frame as returned by \code{fd_tacsat_clean()} — \code{SI_LONG} and
\code{SI_LATI} are plain numeric columns at this stage. Data must be sorted by
\code{VE_REF} and \code{SI_DATIM} (also handled by \code{fd_tacsat_clean()}). Convert to
\code{sf} later in the pipeline when spatial operations are needed.

\strong{This function never filters.} Filtering is deliberately left to the caller:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{tacsat |>
  fd_check_tacsat() |>
  dplyr::filter(checks == "ok")
}\if{html}{\out{</div>}}

Check labels (in priority order):
\describe{
\item{\code{"00 1 no country id"}}{\code{VE_COU} is \code{NA}}
\item{\code{"00 2 no vessel id"}}{\code{VE_REF} is \code{NA}}
\item{\code{"00 3 no time"}}{\code{SI_DATIM} is \code{NA}}
\item{\code{"00 4 no position"}}{\code{SI_LONG} or \code{SI_LATI} is \code{NA}}
\item{\code{"00 5 no speed"}}{\code{SI_SP} is \code{NA}}
\item{\code{"02 duplicates"}}{Duplicate of a previous record with the same vessel, position, and timestamp}
\item{\code{"03 coordinates out of bound"}}{Longitude outside ±180 or latitude outside ±90}
\item{\code{"04 time interval too short"}}{Time since previous ping (per vessel) is less than \code{it_min} seconds}
\item{\code{"ok"}}{All checks passed}
}
}
\examples{
\dontrun{
tacsat |>
  fd_clean_tacsat() |>
  fd_check_tacsat() |>
  dplyr::count(checks)
}

}
