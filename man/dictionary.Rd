% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{dictionary}
\alias{dictionary}
\title{Field definitions for TACSAT2 and EFLALO2}
\format{
A tibble with 47 rows and 7 variables:
\describe{
\item{\code{field}}{Variable name in R (character). Species-specific columns
are represented as \verb{LE_KG_<SP>} and \verb{LE_EURO_<SP>}.}
\item{\code{table}}{Which table the field belongs to: \code{"tacsat"}, \code{"eflalo"},
or \code{"both"} (character).}
\item{\code{type}}{Expected R type after import / cleaning: \code{"chr"}, \code{"dbl"},
\code{"int"}, \code{"dttm"}, \code{"date"}, or \code{"lgl"} (character).}
\item{\code{format}}{Raw format in the original file, e.g. \code{"DD/MM/YYYY"} or
\code{"Decimal degrees"}. \code{NA} for derived columns (character).}
\item{\code{description}}{Short description of the field (character).}
\item{\code{required}}{\code{TRUE} if the field is required by \verb{fd_clean_*} or the
data-call; \code{FALSE} if optional (logical).}
\item{\code{derived}}{\code{TRUE} if the field is added or transformed by
\code{fd_clean_tacsat()} / \code{fd_clean_eflalo()}; \code{FALSE} if present in the
raw file (logical).}
}
}
\source{
ICES VMS and Logbook Data-Call format specification. Derived columns
documented from \code{fd_clean_tacsat()} and \code{fd_clean_eflalo()} source code.
Built via \code{data-raw/DATASET_field_definitions.R}.
}
\usage{
dictionary
}
\description{
A data dictionary covering all fields in the TACSAT2 and EFLALO2 formats as
specified by the ICES VMS and Logbook Data-Call, plus the derived columns
added by \code{fd_clean_tacsat()} and \code{fd_clean_eflalo()}.

Useful for programmatically checking that user-supplied data frames contain
the expected columns with the expected types, and for documentation.
}
\keyword{datasets}
