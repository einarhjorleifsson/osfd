% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_checks.R
\name{fd_eflalo_check}
\alias{fd_eflalo_check}
\title{Single-pass EFLALO quality checks ("myway")}
\usage{
fd_eflalo_check(
  eflalo,
  year = NULL,
  gear = icesVocab::getCodeList("GearType")$Key,
  met6 = icesVocab::getCodeList("Metier6_FishingActivity")$Key
)
}
\arguments{
\item{eflalo}{A data frame in EFLALO format. Must contain \code{VE_REF}, \code{LE_ID},
\code{LE_CDAT} (Date), \code{FT_DDATIM}, \code{FT_LDATIM}, \code{LE_GEAR}, and \code{LE_MET}.
Run \code{fd_eflalo_clean()} before calling this.}

\item{year}{Integer. Submission year. When supplied, records with
\code{FT_DDATIM} before 1 January of this year are flagged as
\code{"06 before submission year"}. If \code{NULL} (default), the check is skipped.}

\item{gear}{Character vector of valid ICES gear codes (metier level 4).
Obtain via \code{icesVocab::getCodeList("GearType")$Key}. If \code{NULL} (default),
the gear check is skipped.}

\item{met6}{Character vector of valid ICES metier level 6 codes.
Obtain via \code{icesVocab::getCodeList("Metier6_FishingActivity")$Key}. If
\code{NULL} (default), the metier check is skipped.}
}
\value{
The input data frame with a \code{checks} character column appended.
}
\description{
Adds a \code{checks} column to an EFLALO data frame in a single \code{case_when()} pass.
Each record is labelled with the first failing check, or \code{"ok"}. The
approach mirrors the "myway" design in
\code{documentation/dataflow_dplyr_thenduckdb.qmd}.

\strong{This function never filters.} Filtering is deliberately left to the caller:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{eflalo |>
  fd_check_eflalo(iv_gear, iv_met6) |>
  dplyr::filter(startsWith(checks, "ok"))
}\if{html}{\out{</div>}}

Note that some checks are labelled \code{"ok - ..."} (informational only) and will
survive a \code{startsWith(checks, "ok")} filter. Use exact matching if you want
to drop those too.

The function assumes \code{FT_DDATIM}, \code{FT_LDATIM} (POSIXct), and \code{LE_CDAT}
(Date) are already present — run \code{fd_eflalo_clean()} first and it handles
all of that so you don't have to.

Check labels (in priority order):
\describe{
\item{\code{"01 duplicated events"}}{Duplicate of a previous row with the same \code{VE_REF}, \code{LE_ID}, and \code{LE_CDAT}}
\item{\code{"02 impossible time"}}{\code{FT_DDATIM} or \code{FT_LDATIM} is \code{NA}}
\item{\code{"03 new years trip"}}{Departure year is exactly one less than landing year (trip crosses year boundary)}
\item{\code{"04 departure after arrival"}}{\code{FT_DDATIM} is strictly later than \code{FT_LDATIM} — time travel not supported}
\item{\code{"05 departure equals arrival"}}{\code{FT_DDATIM == FT_LDATIM} — a trip of zero duration raises an eyebrow}
\item{\code{"06 before submission year"}}{\code{FT_DDATIM} falls before 1 January of \code{year} (only checked when \code{year} is supplied)}
\item{\code{"07 gear (metier 4) invalid"}}{\code{LE_GEAR} is not in \code{iv_gear} (if supplied)}
\item{\code{"08 metier 6 invalid"}}{\code{LE_MET} is not in \code{iv_met6} (if supplied)}
\item{\code{"09 no vessel length"}}{\code{VE_LEN} is \code{NA} — vessel length is required for gear width and swept area calculations}
\item{\code{"10 no vessel tonnage"}}{\code{VE_TON} is \code{NA}}
\item{\code{"ok - 11 no engine power"}}{\code{VE_KW} is \code{NA} (informational — gear width will fall back to lookup table defaults)}
\item{\code{"ok - 12 no mesh size"}}{\code{LE_MSZ} is \code{NA} (informational)}
\item{\code{"ok - 13 catch date before departure"}}{\code{LE_CDAT} precedes \code{FT_DDATIM} (informational)}
\item{\code{"ok - 14 catch date after arrival"}}{\code{LE_CDAT} is later than \code{FT_LDATIM} (informational)}
\item{\code{"ok"}}{All checks passed}
}

Labels prefixed \code{"ok - ..."} are informational. Records with these labels survive
\code{dplyr::filter(startsWith(checks, "ok"))} but are dropped by
\code{dplyr::filter(checks == "ok")} if you want to be strict.

Check "overlapping trips" is intentionally absent — it requires a
separate per-vessel pass. Use \code{fd_overlapping_trips()} for that.
}
\examples{
\dontrun{
eflalo |>
  fd_clean_eflalo() |>
  dplyr::count(checks)
}

}
