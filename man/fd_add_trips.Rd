% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_to_pings.R
\name{fd_add_trips}
\alias{fd_add_trips}
\title{Add Trip Information to Vessel Tracking Data}
\usage{
fd_add_trips(ais, trips, cn = c("tid", ".tid"), remove = TRUE)
}
\arguments{
\item{ais}{A data frame of vessel tracking positions as returned by
\code{fd_clean_tacsat()}. Must contain \code{cid}, \code{vid}, and \code{time} (POSIXct).}

\item{trips}{A data frame of fishing trips as returned by \code{fd_trips()}.
Must contain \code{cid}, \code{vid}, \code{tid}, \code{T1}, and \code{T2}.}

\item{cn}{Character vector of additional column names from \code{trips} to carry
across to \code{ais}. \code{tid} and \code{.tid} is always included. \code{cid} and \code{vid} are implicit
join keys; \code{T1} and \code{T2} are used for the time-overlap join and removed
when \code{remove = TRUE}. Default: \code{"tid"}.}

\item{remove}{Logical. If \code{TRUE} (default), removes \code{T1} and \code{T2} from the
output after the join.}
}
\value{
\code{ais} with \code{tid} (and any additional \code{cn} columns) appended.
Positions that fall outside any trip window have \code{NA} for those columns.
}
\description{
Joins vessel tracking positions (VMS/AIS) with trip-level information from
the logbook based on vessel identity and time overlap. Each position record
is matched to the trip active at that timestamp; positions outside any trip
window receive \code{NA} for the trip columns.
}
\details{
A many-to-one left join: each position is matched to at most one trip based
on vessel (\code{cid}, \code{vid}) and timestamp (\code{time} between \code{T1} and \code{T2}). Only
the columns in \code{cn} (plus the join keys and \code{T1}/\code{T2}) are taken from
\code{trips}, so extra trip columns do not bleed into the output.
}
\examples{
\dontrun{
ais   <- fd_clean_tacsat(tacsat_raw)
trips <- fd_clean_eflalo(eflalo_raw) |> fd_trips()
ais   <- fd_add_trips(ais, trips)

# Carry additional vessel metadata across
ais <- fd_add_trips(ais, trips, cn = c("tid", "length", "kw", "gt"))
}

}
\seealso{
\code{\link[=fd_trips]{fd_trips()}} to extract the trip table from a cleaned eflalo data
frame, \code{\link[=fd_clean_tacsat]{fd_clean_tacsat()}} for the expected input format of \code{ais}.
}
