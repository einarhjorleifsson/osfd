% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gear.R
\name{fd_add_sa}
\alias{fd_add_sa}
\title{Add swept area to VMS pings}
\source{
Adapted from the ICES VMS and Logbook Data Call workflow:
\url{https://github.com/ices-eg/ICES-VMS-and-Logbook-Data-Call}
}
\usage{
fd_add_sa(
  x,
  gear_name = "gear",
  intv_name = ".intv",
  gearwidth_name = ".gearwidth",
  speed_name = "speed"
)
}
\arguments{
\item{x}{A data frame of VMS pings. Must contain the columns named by
\code{gear_name}, \code{intv_name}, \code{gearwidth_name}, and \code{speed_name}.}

\item{gear_name}{Character. Column name for the gear code (métier level 4).
Default \code{"gear"} (the name used after \code{\link[=fd_clean_eflalo]{fd_clean_eflalo()}}).}

\item{intv_name}{Character. Column name for the ping interval \strong{in
seconds}. Default \code{".intv"} (as produced by \code{\link[=fd_interval_seconds]{fd_interval_seconds()}}).}

\item{gearwidth_name}{Character. Column name for gear width \strong{in km}.
Default \code{".gearwidth"} (as produced by \code{\link[=fd_add_gearwidth]{fd_add_gearwidth()}}).}

\item{speed_name}{Character. Column name for fishing speed \strong{in knots}.
Default \code{"speed"} (the name used after \code{\link[=fd_clean_tacsat]{fd_clean_tacsat()}}).}
}
\value{
\code{x} with one additional column \code{.sa} (numeric, km²). \code{NA} where
gear width or speed is unavailable.
}
\description{
Calculates the area swept (km²) by the fishing gear for each VMS ping and
appends the result as a new column \code{.sa}. The calculation is dispatched by
gear type:
\itemize{
\item \strong{Danish seine} (\code{SDN}): rope-loop geometry
(\code{\link[sfdSAR:surface-models]{sfdSAR::danish_seine_contact()}}).
\item \strong{Scottish seine} (\code{SSC}): rope-loop geometry with a splitting-phase
multiplier (\code{\link[sfdSAR:surface-models]{sfdSAR::scottish_seine_contact()}}).
\item \strong{All other gears}: standard trawl formula —
\verb{SA = gear_width × fishing_hours × fishing_speed × 1.852}
(\code{\link[sfdSAR:surface-models]{sfdSAR::trawl_contact()}}).
}

For seines, \code{gear_width} represents total rope length (which may exceed
6 km), not net width.
}
\note{
Requires \code{sfdSAR}. See the WGSFD 2025 Report for a full discussion of
the swept-area methodology:
\url{https://doi.org/10.17895/ices.pub.3073475}
}
\examples{
\dontrun{
ais2 <- ais2 |>
  fd_add_gearwidth() |>
  fd_add_sa()
}

}
