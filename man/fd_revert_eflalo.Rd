% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_clean.R
\name{fd_revert_eflalo}
\alias{fd_revert_eflalo}
\title{Revert EFLALO column names to ICES originals}
\usage{
fd_revert_eflalo(eflalo)
}
\arguments{
\item{eflalo}{A data frame as returned by \code{fd_clean_eflalo()}.}
}
\value{
\code{eflalo} with ICES ALLCAPS column names where applicable, and
\code{FT_DDAT} / \code{FT_DTIME} / \code{FT_LDAT} / \code{FT_LTIME} restored from the
corresponding datetime columns.
}
\description{
Translates the short lowercase column names produced by \code{fd_clean_eflalo()}
back to their original ICES ALLCAPS equivalents (e.g. \verb{vid → VE_REF},
\verb{T1 → FT_DDATIM}). Only columns that are actually present in \code{eflalo} are
renamed; any extra columns are left untouched.

\code{FT_DDAT} / \code{FT_DTIME} and \code{FT_LDAT} / \code{FT_LTIME} are reconstructed from
\code{FT_DDATIM} and \code{FT_LDATIM} respectively, if those columns are present, and
placed immediately after their source column.
}
\examples{
\dontrun{
eflalo_orig <- fd_revert_eflalo(eflalo)
}

}
\seealso{
\code{\link[=fd_clean_eflalo]{fd_clean_eflalo()}}, \code{\link[=fd_translate]{fd_translate()}}
}
