% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_clean.R
\name{fd_check_input}
\alias{fd_check_input}
\title{Validate a TACSAT or EFLALO data frame against the field definitions}
\usage{
fd_check_input(data, which = c("tacsat", "eflalo"), dictionary = fd_dictionary)
}
\arguments{
\item{data}{A data frame in TACSAT or EFLALO format (raw, before cleaning).}

\item{which}{Character. Which format to validate against: \code{"tacsat"} or
\code{"eflalo"}.}

\item{dictionary}{A data frame (or tibble) with at least the columns \code{old},
\code{table}, \code{required}, \code{derived}, and \code{type}. Defaults to the package
dictionary \code{fd_dictionary}, which covers all standard TACSAT2 and EFLALO2
fields. Override only if you are working with a custom field set.}
}
\value{
\code{data}, invisibly.
}
\description{
Checks a raw TACSAT or EFLALO data frame against the \code{dictionary}
dictionary before cleaning. Three things are checked:
\enumerate{
\item \strong{Required fields} -- stops with an informative error if any are absent.
\item \strong{Optional fields} -- emits a message listing any that are absent
(processing continues).
\item \strong{Coercion safety} -- warns if converting a field to its target R type
(numeric or Date) would silently introduce new \code{NA} values, indicating
malformed values in the raw data.
}

The function returns \code{data} invisibly, so it can be used in a pipe directly
upstream of \code{fd_clean_tacsat()} / \code{fd_clean_eflalo()}, or called standalone
as a preflight check.

Species-level catch columns (\verb{LE_KG_<SP>}, \verb{LE_EURO_<SP>}) are not checked.
}
\examples{
\dontrun{
# Standalone preflight check
fd_check_input(tacsat_raw, "tacsat")

# In a pipe (check then clean in one step)
tacsat <- tacsat_raw |>
  fd_check_input("tacsat") |>
  fd_clean_tacsat()
}

}
\seealso{
\link{fd_dictionary} for the underlying dictionary,
\code{\link[=fd_clean_tacsat]{fd_clean_tacsat()}} and \code{\link[=fd_clean_eflalo]{fd_clean_eflalo()}} which call this internally.
}
