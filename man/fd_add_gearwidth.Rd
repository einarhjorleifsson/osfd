% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gear.R
\name{fd_add_gearwidth}
\alias{fd_add_gearwidth}
\title{Add gear width to VMS pings}
\source{
Adapted from the ICES VMS and Logbook Data Call workflow:
\url{https://github.com/ices-eg/ICES-VMS-and-Logbook-Data-Call}
}
\usage{
fd_add_gearwidth(x, met_name = "met6", oal_name = "length", kw_name = "kw")
}
\arguments{
\item{x}{A data frame of VMS pings. Must contain the columns named by
\code{met_name}, \code{oal_name}, and \code{kw_name}.}

\item{met_name}{Character. Column name for métier level 6. Default \code{"met6"}
(the name used after \code{\link[=fd_clean_eflalo]{fd_clean_eflalo()}}).}

\item{oal_name}{Character. Column name for overall vessel length (metres).
Default \code{"length"} (the name used after \code{\link[=fd_clean_eflalo]{fd_clean_eflalo()}}).}

\item{kw_name}{Character. Column name for engine power (kW). Default \code{"kw"}
(the name used after \code{\link[=fd_clean_eflalo]{fd_clean_eflalo()}}).}
}
\value{
\code{x} with one additional column \code{.gearwidth} (numeric, km). \code{NA}
where no value could be determined.
}
\description{
Predicts gear contact width (km) for each VMS ping using the BENTHIS model
and appends the result as a new column \code{.gearwidth}. The fill priority is:
\enumerate{
\item User-supplied \code{LE_GEARWIDTH} (if the column is present and not \code{NA}).
\item Model prediction from \code{\link[sfdSAR:predict_gear_width]{sfdSAR::predict_gear_width()}} (converted from
metres to km).
\item BENTHIS lookup-table default (\code{gearWidth}).
}

The lookup table is built internally via \code{\link[=fd_benthis_lookup]{fd_benthis_lookup()}}, which
fetches the RCG métier reference list from GitHub at runtime.
}
\note{
Requires \code{sfdSAR} and \code{icesVMS}. Fetches the métier reference list
from GitHub at runtime — an internet connection is required.
}
\examples{
\dontrun{
ais2 <- ais2 |> fd_add_gearwidth()

# Non-default column names (raw ICES names)
ais2 <- ais2 |>
  fd_add_gearwidth(met_name = "LE_MET", oal_name = "VE_LEN", kw_name = "VE_KW")
}

}
