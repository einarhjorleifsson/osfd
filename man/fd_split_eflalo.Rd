% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_checks.R
\name{fd_split_eflalo}
\alias{fd_split_eflalo}
\title{Split EFLALO into trip and event tables}
\usage{
fd_split_eflalo(eflalo)
}
\arguments{
\item{eflalo}{A data frame in EFLALO format.}
}
\value{
A named list with two elements:
\describe{
\item{\code{trips}}{A tibble with columns \code{VE_COU}, \code{VE_REF}, \code{VE_LEN},
\code{VE_KW}, \code{VE_TON}, \code{FT_REF}, \code{FT_DDATIM}, \code{FT_LDATIM}. One row per
unique trip (\code{VE_REF} × \code{FT_REF}).}
\item{\code{events}}{A tibble with columns \code{VE_COU}, \code{VE_REF}, \code{FT_REF},
and all \verb{LE_*} columns from the input.}
}
}
\description{
Splits an EFLALO data frame into two tidy tables:
\describe{
\item{\code{trips}}{One row per fishing trip. Contains vessel characteristics
and departure/landing datetimes.}
\item{\code{events}}{One row per log event. Contains gear, catch, and date
information.}
}

Separating the two concerns up front makes downstream joins explicit and
SQL-compatible — no more wondering which column belongs to which level of
aggregation.

The function expects \code{FT_DDATIM} and \code{FT_LDATIM} POSIXct columns to be
present (run \code{fd_eflalo_clean()} first if needed).
}
\examples{
\dontrun{
parts  <- fd_split_eflalo(eflalo)
trips  <- parts$trips
events <- parts$events
}

}
