% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gear.R
\name{fd_benthis_lookup}
\alias{fd_benthis_lookup}
\title{Build the BENTHIS gear-width lookup table}
\source{
Adapted from the ICES VMS and Logbook Data Call workflow:
\url{https://github.com/ices-eg/ICES-VMS-and-Logbook-Data-Call}
}
\usage{
fd_benthis_lookup(kw_name = "kw", oal_name = "length")
}
\arguments{
\item{kw_name}{Character. Name of the engine-power column in the VMS data
frame. Replaces the \code{"avg_kw"} sentinel in \code{gearCoefficient}. Default
\code{"kw"} (the name used after \code{\link[=fd_clean_eflalo]{fd_clean_eflalo()}}).}

\item{oal_name}{Character. Name of the overall-length column in the VMS data
frame. Replaces the \code{"avg_oal"} sentinel in \code{gearCoefficient}. Default
\code{"length"} (the name used after \code{\link[=fd_clean_eflalo]{fd_clean_eflalo()}}).}
}
\value{
A data frame with one row per unique \code{Metier_level6} × BENTHIS métier
combination and columns:
\code{Metier_level6}, \code{benthisMet}, \code{avKw}, \code{avLoa}, \code{avFspeed},
\code{subsurfaceProp}, \code{gearWidth}, \code{firstFactor}, \code{secondFactor},
\code{gearModel}, \code{gearCoefficient}, \code{contactModel}.
}
\description{
Fetches the RCG métier reference list from the ICES GitHub repository and
joins it with the BENTHIS gear-width parameters from \code{\link[icesVMS:get_benthis_parameters]{icesVMS::get_benthis_parameters()}}.
The result is a lookup table keyed on \code{Metier_level6} that is used
downstream by gear-width prediction to map each VMS ping to its BENTHIS
model coefficients.

The \code{gearCoefficient} column in the raw BENTHIS table contains two sentinel
strings (\code{"avg_kw"}, \code{"avg_oal"}) that reference whichever columns in the
VMS data hold engine power and vessel length. These are replaced here with
the actual column names supplied via \code{kw_name} and \code{oal_name} so that
\code{\link[sfdSAR:predict_gear_width]{sfdSAR::predict_gear_width()}} can address them directly.
}
\note{
Requires \code{icesVMS}. Fetches the métier reference list from GitHub at
runtime — an internet connection is required.
}
\examples{
\dontrun{
aux_lookup <- fd_benthis_lookup()

# If your VMS data uses non-default column names:
aux_lookup <- fd_benthis_lookup(kw_name = "VE_KW", oal_name = "VE_LEN")
}

}
