% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trail_steps.R
\name{fd_interval_seconds}
\alias{fd_interval_seconds}
\title{Ping interval in seconds, with an upper cap}
\usage{
fd_interval_seconds(time, probs = 0.975)
}
\arguments{
\item{time}{A POSIXct datetime vector, sorted chronologically within any
potential groups.}

\item{probs}{Numeric scalar in \verb{[0, 1]}. The quantile of the computed
intervals used as the upper cap. Default \code{0.975}.}
}
\value{
A numeric vector of ping intervals (seconds), the same length as
\code{time}, with values capped at the \code{probs} quantile.
}
\description{
Computes the time interval (in seconds) between consecutive pings via
\code{\link[=fd_step_time]{fd_step_time()}}, then caps any value that exceeds the \code{probs} quantile of
the computed intervals.

\strong{Chronological order and grouping are the responsibility of the caller.}
Call this function inside \code{\link[dplyr:mutate]{dplyr::mutate()}} after \code{\link[dplyr:group_by]{dplyr::group_by()}} on
the trip identifier.
}
\examples{
\dontrun{
ais |>
  dplyr::group_by(.tid) |>
  dplyr::mutate(.intv = fd_interval_seconds(time)) |>
  dplyr::ungroup()

# Use 99th percentile as the cap
ais |>
  dplyr::group_by(.tid) |>
  dplyr::mutate(.intv = fd_interval_seconds(time, probs = 0.99)) |>
  dplyr::ungroup()
}

}
