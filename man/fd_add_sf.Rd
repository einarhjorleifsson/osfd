% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo.R
\name{fd_add_sf}
\alias{fd_add_sf}
\title{Spatially join an sf polygon layer onto AIS/VMS pings}
\usage{
fd_add_sf(ais, shape)
}
\arguments{
\item{ais}{Data frame or \code{sf} object of VMS/AIS pings. If not already \code{sf},
must contain columns \code{lon} and \code{lat}.}

\item{shape}{An \code{sf} polygon object to join onto \code{ais}.}
}
\value{
An \code{sf} object with the same number of rows as \code{ais}, augmented with
columns from \code{shape}.
}
\description{
Converts \code{ais} to an \code{sf} point object (if not already) using columns \code{lon}
and \code{lat} (CRS 4326), then performs a spatial left join with \code{shape} via
\code{\link[sf:st_join]{sf::st_join()}}. A row-count guard stops if the join inflates the number of
rows, which would indicate a one-to-many match (overlapping polygons).
}
\note{
The row-count guard stops with an uninformative message if \code{shape}
contains overlapping polygons causing a one-to-many join. Consider
using \code{\link[sf:st_join]{sf::st_join()}} with \code{largest = TRUE} or pre-dissolving overlaps
if this is a concern.
}
