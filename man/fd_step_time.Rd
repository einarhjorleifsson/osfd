% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trail_steps.R
\name{fd_step_time}
\alias{fd_step_time}
\title{Trail step time}
\usage{
fd_step_time(datetime, weight = c(1, 0), fill_na = TRUE)
}
\arguments{
\item{datetime}{A POSIXct datetime vector, should be sorted chronologically within groups.}

\item{weight}{Numeric vector of length 2: weights for the backward and
forward time differences respectively. Normalised to sum to 1. Default
\code{c(1, 0)} assigns the full interval backward (time since the last ping).}

\item{fill_na}{Logical. If \code{TRUE}, boundary \code{NA}s (first ping has no backward
diff; last ping has no forward diff) are filled with the available
one-sided value. Default \code{TRUE}.}
}
\value{
A numeric vector (seconds) of the same length as \code{datetime}.
}
\description{
Computes the time interval (in seconds) between consecutive pings.
Use a weighted blend of time since the previous ping and time to the next ping.

\strong{Chronological order is the responsibility of the caller}.

\strong{Grouping must be done by the caller.} This function operates on a single
datetime vector; use it within \code{\link[dplyr:mutate]{dplyr::mutate()}} after grouping.
}
\examples{
\dontrun{
# After fd_clean_tacsat() the datetime column is `time` and vessel id is `vid`
tacsat |>
  dplyr::group_by(vid) |>
  dplyr::mutate(.intv = fd_step_time(time)) |>
  dplyr::ungroup()

# equal forward/backward blend, fill boundary NAs
tacsat |>
  dplyr::group_by(vid) |>
  dplyr::mutate(.intv = fd_step_time(time, weight = c(0.5, 0.5), fill_na = TRUE)) |>
  dplyr::ungroup()
}

}
