% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo.R
\name{d2ir}
\alias{d2ir}
\title{Convert decimal degree coordinates to ICES statistical rectangle codes}
\usage{
d2ir(lon, lat, sub = FALSE)
}
\arguments{
\item{lon}{Numeric vector of longitudes (decimal degrees, WGS84).}

\item{lat}{Numeric vector of latitudes (decimal degrees, WGS84).}

\item{sub}{Logical scalar. If \code{TRUE}, appends a fifth character (1–9)
identifying the sub-rectangle within the standard rectangle. The ICES
sub-rectangle system divides each 1° × 0.5° rectangle into a 3 × 3 grid
of 20' × 10' cells, numbered north-to-south within each west-to-east
column:

\if{html}{\out{<div class="sourceCode">}}\preformatted{1  4  7
2  5  8
3  6  9
}\if{html}{\out{</div>}}

Defaults to \code{FALSE}.}
}
\value{
A character vector of ICES rectangle codes (e.g., \code{"41E9"}), or
five-character sub-rectangle codes (e.g., \code{"41E93"}) when \code{sub = TRUE}.
Returns \code{NA} for positions outside the ICES statistical area
(lat < 36, lat ≥ 85.5, lon ≤ -44, lon > 68.5).
}
\description{
Converts longitude/latitude pairs to ICES statistical rectangle codes.
Per the ICES specification
(\url{https://www.ices.dk/data/maps/Pages/ICES-statistical-rectangles.aspx}),
latitudinal rows have 30' intervals (numbered from 01 at 36°N), and
longitudinal columns have 1° intervals — with zone A covering only
44°W–40°W (codes A0–A3), after which zones B onwards each span a full 10°.
}
\details{
A small offset (\code{1e-6}) is added to both inputs so that positions exactly on
a rectangle boundary are assigned to the eastern/northern rectangle,
matching the convention of \code{geo::d2ir}.

All operations translate to SQL, making the function compatible with
\code{\link[dplyr:mutate]{dplyr::mutate()}} on both in-memory data frames and remote/lazy database
tables (e.g., DuckDB via \code{duckdbfs}).
}
\examples{
d2ir(-5.0, 63.2)            # "55E5"
d2ir(-5.0, 63.2, sub = TRUE) # "55E52" (middle-west sub-rectangle)
d2ir(-43.5, 64.0)           # "57A0"  (zone A: 4-degree sub-divisions)

# Works inside mutate on local or lazy frames:
tibble::tibble(
  lon = c(-5.3, 10.1, -43.5),
  lat = c(63.2,  58.7,  64.0)
) |>
  dplyr::mutate(ices_rect = d2ir(lon, lat))

}
