% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_flag.R
\name{fd_flag_tacsat}
\alias{fd_flag_tacsat}
\title{Single-pass TACSAT quality checks}
\usage{
fd_flag_tacsat(
  tacsat,
  minimum_interval_seconds = 30,
  areas = ices_areas,
  ports = harbours,
  no_hands = TRUE
)
}
\arguments{
\item{tacsat}{A TACSAT data frame as returned by \code{fd_clean_tacsat()}. Must
contain \code{cid} (\code{VE_COU}), \code{vid} (\code{VE_REF}), \code{time} (\code{SI_DATIM}, POSIXct),
\code{lon} (\code{SI_LONG}), \code{lat} (\code{SI_LATI}), and \code{speed} (\code{SI_SP}).}

\item{minimum_interval_seconds}{Numeric. Minimum permitted time interval
between consecutive pings for the same vessel, in \strong{seconds}. Default: 30.}

\item{areas}{A spatial object (sf) containing the area of interest. If \code{NULL}
(default), the function uses \code{osfd::ices_areas}. Records outside this area
fail the check.}

\item{ports}{A spatial object (sf) containing port locations. If not
already an sf object, it should have \code{lon} and \code{lat} columns. Ports are
automatically buffered to 3 km radius and converted to Web Mercator
(EPSG:3857) for accurate distance calculations before being transformed
back to WGS84 (EPSG:4326). Default: \code{osfd::harbours}.}

\item{no_hands}{Logical. Controls whether failing records are automatically
removed from the output. When \code{TRUE} (default, production mode), records
where \code{.checks != "ok"} are filtered out and the \code{.checks} column is
dropped before returning. When \code{FALSE} (diagnostic mode), all records are
returned with \code{.checks} and \code{.intv} appended so you can inspect failures.}
}
\value{
\itemize{
\item \code{no_hands = TRUE} (default): The input data frame filtered to passing
records only, with \code{.intv} appended and \code{.checks} dropped.
\item \code{no_hands = FALSE}: The full input data frame (geometry dropped) with
two new columns appended:
\describe{
\item{\code{.checks}}{Character. First failing check label, or \code{"ok"}.}
\item{\code{.intv}}{Numeric. Time interval in seconds since the previous
ping for the same vessel.}
}
}
}
\description{
Adds a \code{.checks} column to a TACSAT data frame in a single \code{case_when()} pass.
Each record is labelled with the first failing check, or \code{"ok"} if it passes
all of them. Also computes an \code{.intv} column (time in seconds since the
previous ping for the same vessel) which is used by the interval threshold
check and is useful downstream.

Expects a data frame as returned by \code{fd_clean_tacsat()} — \code{lon} (\code{SI_LONG}) and
\code{lat} (\code{SI_LATI}) are plain numeric columns at this stage. Data must be sorted
by \code{vid} (\code{VE_REF}) and \code{time} (\code{SI_DATIM}), which \code{fd_clean_tacsat()} handles.
The function converts to \code{sf} internally for spatial operations, then drops
geometry before returning.

By default (\code{no_hands = TRUE}) the function filters out failing records and
drops \code{.checks} before returning — suitable for production pipelines where
the cleaned result is all that is needed. Set \code{no_hands = FALSE} to retain
all records with the \code{.checks} label, which is useful for interactive
diagnostics:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{tacsat |>
  fd\\_flag\\_tacsat(no_hands = FALSE) |>
  dplyr::count(.checks)
}\if{html}{\out{</div>}}

Checks are performed in the following order and return the first failure:
\describe{
\item{\code{"00 missing coordinates"}}{\code{lon} or \code{lat} is \code{NA} — checked first because
\code{NA} coordinates would cause \code{sf::st_as_sf()} to error. These records are
separated before spatial processing and rejoined with this label.}
\item{\code{"01 point out of (ices) area"}}{Record falls outside the specified area of interest}
\item{\code{"02 duplicate"}}{Duplicate of a previous record with the same vessel, position, and timestamp}
\item{\code{"03 time interval too short"}}{Time since previous ping (per vessel) is less than \code{minimum_interval_seconds}}
\item{\code{"04 in harbour"}}{Record location falls within a harbour buffer zone (3 km radius)}
\item{\code{"05 no country id"}}{\code{cid} (\code{VE_COU}) is \code{NA}}
\item{\code{"06 no vessel id"}}{\code{vid} (\code{VE_REF}) is \code{NA}}
\item{\code{"07 no datetime"}}{\code{time} (\code{SI_DATIM}) is \code{NA}}
\item{\code{"08 no speed"}}{\code{speed} (\code{SI_SP}) is \code{NA}}
\item{\code{"ok"}}{All checks passed}
}

\strong{Note:} This function is R-only. \code{sf} spatial joins, \code{base::duplicated()},
and \code{fd_step_time()} are not compatible with lazy/DuckDB backends.
}
\examples{
\dontrun{
# Production: get only clean pings
osfd::tacsat |>
  fd_clean_tacsat() |>
  fd_flag_tacsat()

# Diagnostic: inspect all check labels
osfd::tacsat |>
  fd_clean_tacsat() |>
  fd_flag_tacsat(no_hands = FALSE) |>
  dplyr::count(.checks)
}

}
