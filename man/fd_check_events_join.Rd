% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_to_pings.R
\name{fd_check_events_join}
\alias{fd_check_events_join}
\title{Diagnose many-to-many conflicts before joining events to pings}
\usage{
fd_check_events_join(ais, events)
}
\arguments{
\item{ais}{A data frame of vessel tracking positions after
\code{\link{fd_add_trips}} has been applied. Must contain \code{.pid}, \code{.tid},
and \code{time}.}

\item{events}{A data frame of fishing events as returned by
\code{\link{fd_events}} (and optionally \code{\link{fd_flag_events}}).
Must contain \code{.tid}, \code{t1}, and \code{t2}.}
}
\value{
A tibble of the bloated join rows — i.e. only the pings that matched
more than one event, with all event columns attached. Each such ping appears
once per matched event. Returns an empty tibble (invisibly) with a message
when no conflicts exist.
}
\description{
Performs the same join as \code{\link{fd_add_events}} but \strong{without} a
relationship constraint, then returns the subset of rows where a single ping
matched more than one event. Use this before calling \code{fd_add_events()} to
understand why it errors.

The function is intentionally free-standing and makes no assumptions about
\emph{why} the conflict arises — it simply surfaces whatever causes the bloat
(overlapping real times, identical dummy windows, soft duplicates, etc.).
}
\examples{
\dontrun{
ais2 <- fd_add_trips(ais, trips, cn = c("tid", "length", "kw", "gt", ".tid"))

conflicts <- fd_check_events_join(ais2, events)
conflicts |> dplyr::count(.tid, t1, t2, sort = TRUE)
conflicts |> dplyr::distinct(.tid, t1, t2, .eid)
}

}
\seealso{
\code{\link{fd_add_events}}, \code{\link{fd_flag_events}}
}
