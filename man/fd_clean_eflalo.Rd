% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_clean.R
\name{fd_clean_eflalo}
\alias{fd_clean_eflalo}
\title{Validate and coerce EFLALO columns}
\usage{
fd_clean_eflalo(eflalo, remove = TRUE)
}
\arguments{
\item{eflalo}{A data frame in EFLALO format.}

\item{remove}{Logical. If \code{TRUE} (default), the raw date/time columns
\code{FT_DDAT}, \code{FT_DTIME}, \code{FT_LDAT}, and \code{FT_LTIME} are dropped once the
combined \code{FT_DDATIM} / \code{FT_LDATIM} columns have been constructed. Life is
too short to keep hauling around redundant columns that have already done
their one job. Set to \code{FALSE} if you enjoy clutter.}
}
\value{
The input \code{eflalo} as a tibble with numeric columns coerced and
\code{FT_DDATIM} / \code{FT_LDATIM} added. If \code{remove = TRUE}, the now-redundant
raw date/time columns are absent from the result.
}
\description{
Checks that the required vessel, trip, and catch columns are present in an
EFLALO data frame, then gets the types right so nothing downstream has to
guess: numeric columns are coerced to numeric; \code{FT_DDATIM} and \code{FT_LDATIM}
are constructed from the raw date/time string columns; and \code{LE_CDAT} is
parsed from \code{"DD/MM/YYYY"} to Date. Call this before \code{\link[=fd_eflalo_checks]{fd_eflalo_checks()}}.

Value-level sanity checks (missing vessel lengths, implausible engine power,
etc.) live in \code{\link[=fd_eflalo_checks]{fd_eflalo_checks()}} — this function only concerns itself with
structure and type.
}
\examples{
\dontrun{
eflalo <- fd_clean_eflalo(eflalo)
eflalo <- fd_clean_eflalo(eflalo, remove = FALSE) # keep the raw columns
}

}
