% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo.R
\name{fd_calc_csq}
\alias{fd_calc_csq}
\title{Convert longitude/latitude to a c-square code}
\usage{
fd_calc_csq(lon, lat, degrees = 0.05)
}
\arguments{
\item{lon}{Numeric vector of longitudes (decimal degrees, WGS84).}

\item{lat}{Numeric vector of latitudes  (decimal degrees, WGS84).}

\item{degrees}{Resolution. One of \code{10}, \code{5}, \code{1}, \code{0.5}, \code{0.1}, \code{0.05},
\code{0.01}. Default \code{0.05}.}
}
\value{
A character vector of c-square codes (e.g. \code{"7500:104:100:1"} at
0.05°). Returns \code{NA_character_} where \code{lon} or \code{lat} is \code{NA}.
}
\description{
Computes the c-square notation from decimal-degree coordinates at a
user-specified resolution. Based on the c-squares specification v1.1
(Rees, 2005).
}
\details{
The implementation uses only \code{floor()}, \code{abs()}, \code{round()}, integer
arithmetic, \code{paste0()}, and \code{as.character()} / \code{as.integer()}. Despite each
of those primitives having a SQL analogue, \code{fd_calc_csq()} is an R function
and \strong{cannot} be called inside \code{\link[dplyr:mutate]{dplyr::mutate()}} on a lazy DuckDB /
\code{duckdbfs} table — DuckDB will error because the function is not registered
as a SQL scalar function. For DuckDB-compatible spatial gridding at a single
resolution, use the midpoint formula (\code{lon \%/\% dx * dx + dx/2}) inline.

The global quadrant encoding follows the WMO convention: 1 = NE, 3 = SE,
5 = SW, 7 = NW.  Intermediate quadrant digits (1–4) encode whether the
latitude and longitude digits within each cycle are "low" (0–4) or "high"
(5–9): 1 = both low, 2 = lat low / lon high, 3 = lat high / lon low,
4 = both high.
}
\examples{
fd_calc_csq(-4, 50, 0.01)   # "7500:104:100:100"
fd_calc_csq(-4, 50, 0.05)   # "7500:104:100:1"
fd_calc_csq(-4, 50, 1)      # "7500:104"
fd_calc_csq(NA, 50, 0.05)   # NA

# Works inside mutate on local or lazy frames:
tibble::tibble(lon = c(-4, 10), lat = c(50, 55)) |>
  dplyr::mutate(csq = fd_calc_csq(lon, lat, 0.05))

}
\references{
\url{https://www.marine.csiro.au/csquares/spec1-1.htm}
}
\seealso{
\code{\link[=csq2lonlat]{csq2lonlat()}} for the inverse.
}
