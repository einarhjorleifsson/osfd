% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_clean.R
\name{fd_revert_tacsat}
\alias{fd_revert_tacsat}
\title{Revert TACSAT column names to ICES originals}
\usage{
fd_revert_tacsat(tacsat)
}
\arguments{
\item{tacsat}{A data frame as returned by \code{fd_clean_tacsat()}.}
}
\value{
\code{tacsat} with ICES ALLCAPS column names where applicable, and
\code{SI_DATE} / \code{SI_TIME} restored from \code{SI_DATIM}.
}
\description{
Translates the short lowercase column names produced by \code{fd_clean_tacsat()}
back to their original ICES ALLCAPS equivalents (e.g. \verb{vid → VE_REF},
\verb{time → SI_DATIM}). Only columns that are actually present in \code{tacsat} are
renamed; any extra columns are left untouched.

\code{SI_DATE} (\code{"DD/MM/YYYY"}) and \code{SI_TIME} (\code{"HH:MM"}) are reconstructed from
\code{SI_DATIM} if that column is present, and placed immediately after it.
}
\examples{
\dontrun{
tacsat_orig <- fd_revert_tacsat(tacsat)
}

}
\seealso{
\code{\link[=fd_clean_tacsat]{fd_clean_tacsat()}}, \code{\link[=fd_translate]{fd_translate()}}
}
