% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{tacsat}
\alias{tacsat}
\title{TACSAT example VMS dataset}
\format{
A data frame with 97,015 rows and 8 variables:
\describe{
\item{VE_COU}{Flag nation — \code{"Atlantis"} throughout (character)}
\item{VE_REF}{Vessel reference / ID (character)}
\item{SI_LATI}{Latitude, decimal degrees (numeric)}
\item{SI_LONG}{Longitude, decimal degrees (numeric)}
\item{SI_DATE}{Date, \code{DD/MM/YYYY} (character)}
\item{SI_TIME}{Time (UTC), \code{HH:MM} (character)}
\item{SI_SP}{Instantaneous speed, knots (numeric)}
\item{SI_HE}{Instantaneous heading, degrees (numeric)}
}
}
\source{
\code{vmstools} R package, EU Lot 2 project.
Contact: Niels T. Hintzen \email{niels.hintzen@wur.nl}.
Modified for \code{osfd} via \code{data-raw/DATASET_vnstools.R}.
}
\usage{
tacsat
}
\description{
A synthetic Vessel Monitoring System (VMS) dataset in TACSAT2 format,
originally distributed with the \code{vmstools} package (EU Lot 2 project).
Covers the same fictional "Atlantis" fleet as \link{eflalo} over two consecutive
years (1803–1804).

The dataset was modified from the \code{vmstools} original by shifting years from
1800–1801 to 1803–1804, for the same reason as \link{eflalo}: 1800 is not a
Gregorian leap year, which caused silent errors in date-difference
calculations.

Date and time are stored as character strings (\code{SI_DATE} as \code{DD/MM/YYYY},
\code{SI_TIME} as \code{HH:MM}). Use \code{fd_tacsat_clean()} to coerce columns and parse
\code{SI_DATIM} (a combined POSIXct datetime).
}
\note{
This dataset is provided for example and testing purposes only. It is
derived from the \code{vmstools} package, where it is described as: \emph{"Without
prior permission of the authors it is not allowed to use this data other
than for example non-publishable purposes."} The original data are of
disguised origin and do not represent real fishing activity.
}
\seealso{
\link{eflalo}, \code{\link[=fd_tacsat_clean]{fd_tacsat_clean()}}, \code{\link[=fd_tacsat_checks]{fd_tacsat_checks()}}
}
\keyword{datasets}
